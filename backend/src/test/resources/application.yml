spring:
  datasource:
    url: jdbc:h2:mem:stocklookup-test;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

app:
  jwt:
    secret: test-secret-test-secret-test-secret-12345
    expiry-minutes: 120
  finnhub:
    base-url: https://finnhub.io/api/v1
    api-key: test-key
